import 'package:flutter/material.dart';

void main() => runApp(RoomChat());

class RoomChat extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('ROOM Chat')),
    );
  }
}